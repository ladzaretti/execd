Usage:
  ./bin/execd [flags]
  ./bin/execd config generate
  ./bin/execd version

The execd daemon exposes configured commands as authenticated HTTP endpoints.

  -config string
    	config file path (default: $XDG_CONFIG_HOME/.execd.toml; ~/.config/.execd.toml when unset)
